# Developer Scripts

Helper scripts for common development tasks.

- `dev.sh` — start backend (uvicorn) and frontend (vite) together in dev mode.
- `test.sh` — run the backend pytest suite and the frontend build check.

Scripts assume the repo-root `venv` exists (see README quick start).
