# QUANTUM-AGIS backend dependencies
# NOTE: The quantum core is pure NumPy/SciPy — qutip was removed from the
# runtime requirements (it is unused and has no wheels for newer CPython).
# Install it locally with `pip install qutip` if you need QuTiP notebooks.
# NOTE: numpy/scipy pins were bumped from the original 2024 snapshot because
# the originals have no prebuilt wheels for the CPython versions in use;
# every pin below ships prebuilt wheels for Python 3.11 (no compilers needed).
fastapi==0.109.0
uvicorn[standard]==0.27.0
numpy==2.1.3
scipy==1.14.1
pydantic==2.9.2
pydantic-settings==2.6.1
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx==0.26.0
websockets==12.0
python-multipart==0.0.6
